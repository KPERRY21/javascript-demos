export function demo() {

const num1 = 100;
const num2 = 50;//If I change this to 60. the answer outputs to a double instead of an int in the division
let val;

// Simple math with numbers
val = num1 + num2;//Adding the 2 consts
// val = num1 * num2;//Mutiply the 2 consts
// val = num1 - num2;//Subtract the 2 consts
// val = num1 / num2;//Divide the 2 consts
// val = num1 % num2;//Modulous the 2 consts

// Math Object
// val = Math.PI;
// val = Math.E;
// val = Math.round(2.4);
// val = Math.ceil(2.4);
// val = Math.floor(2.8);
// val = Math.sqrt(64);
// val = Math.abs(-3);
// val = Math.pow(8, 2);
// val = Math.min(2,33,4,1,55,6,3,-2);
// val = Math.max(2,33,4,1,55,6,3,-2);
// val = Math.random();

//val = Math.floor(Math.random() * 100 + 1);//Get the randome nuber first thank myltiply by 100 and add 1. Then you multioply by 100 it means the nmber will be between 0 and 100. but dont want an number of 0 so the plus 1 make its not 0. The floor truncates it to make it a whole number

console.log(val);

}